{
  "version": 2,
  "builds": [
    {
      "src": "Phase2/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/Phase2/dist/$1" }
  ]
}
